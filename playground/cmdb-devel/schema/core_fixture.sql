-- object_types
--     application
--     file
--     host
--     network
--     port
--     configuration_entry

-- relationship_types
--     connected to
--     listens on / connects to (port?)
--     writes to / reads from
--     uses / used by   (requires/optional)

